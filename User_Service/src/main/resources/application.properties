spring.application.name=user



# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/user_service_db
spring.datasource.username=root
spring.datasource.password=FAla12/?
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Security Configuration
spring.security.user.name=topgee
spring.security.user.password=topgee

# JWT Configuration (If you add JWT support in the future)
jwt.secret=your-secret-key
jwt.expiration-ms=3600000

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.topg.user_service=DEBUG  

# Validation Messages (Optional, for custom validation messages)
spring.messages.basename=messages
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka

server.port=7718

